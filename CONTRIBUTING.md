# 贡献指南

感谢参与。这个项目只有三条门槛，都很短。

## 1. 供应商中立（最重要）

- 不要在任何地方写商业产品的名称，不要写「对标 / 兼容 / 参考某某产品」。
- 不要提交带有第三方专有代码或专有文档片段的贡献。
- 需要指代同类思路时，用通用描述，例如「会话驱动的指令自进化」。

## 2. 内核保持纯净

`packages/kernel` 不碰文件系统、不碰网络、不 import 适配器。凡是 IO，都定义成 `ports` 里的接口，由适配器实现。

判断方法：删掉所有适配器，内核仍然能通过 `pnpm --filter @evogen/kernel build`。

## 3. 能被验证

- 提交前跑 `pnpm install && pnpm build`。
- 涉及写入行为的改动，请说明如何撤销，以及你验证撤销的方式。
- 新增能力请同步更新 `docs/roadmap.md` 或对应文档。

## 提交流程

1. 从 `main` 开分支，命名 `codex/<简短描述>`。
2. 提交信息说清「改了什么、怎么验证的」。
3. 开 PR，描述里附上验证命令与输出摘要。
